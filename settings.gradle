pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url = uri("https://mediaadsdk.jfrog.io/artifactory/adsdk/")
            credentials {
                username = "sdk-dev@media.net"
                password = ""
            }
        }
    }
}
rootProject.name = "Test App"
include ':app'
